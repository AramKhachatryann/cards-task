import "./Aside.css";

const Aside = () => {
  return (
    <aside>
      <span> Press the "add card" button to add the new Card.</span>
      <span>
        Use the "sort cards" button to sort the Cards by the increase.
      </span>
      <span> Press an X icon on the top right to delete them.</span>
    </aside>
  );
};

export default Aside;
